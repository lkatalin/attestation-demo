# Single cluster: coco-infra setup + oc login to that cluster.
# cp config/local.env.example config/local.env

export IMAGE=quay.io/YOUR_USER/confidential-inferencing-demo:latest
export ARTIFACTS=./artifacts

# Optional overrides
# export TRUSTEE_NS=trustee-operator-system
# export DEK_SECRET_NAME=confidential-inferencing-dek
